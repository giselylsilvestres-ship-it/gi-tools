const $=x=>document.getElementById(x),sb=supabase.createClient(window.CHECKIN_SUPABASE_URL,window.CHECKIN_SUPABASE_KEY);let user=null,items=[],month=new Date(),selectedDate=null,mindStates=new Set(),soulStates=new Set();const views=[...document.querySelectorAll('.view')],nav=[...document.querySelectorAll('[data-view]')],pad=n=>String(n).padStart(2,'0'),key=d=>`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`,today=()=>key(new Date());const MIND=[['Afiada','rapidez, clareza, conexões'],['Focada','atenção e concentração'],['Produtiva','capacidade de executar'],['Nebulosa','lentidão/confusão cognitiva'],['Travada','dificuldade de começar, decidir ou avançar'],['Exausta','fadiga mental']],SOUL=[['Radiante','feliz, otimista, energizada'],['Serena','tranquila, em paz'],['Divertida','leve, brincalhona, bem-humorada'],['Inquieta','ansiosa, tensa, agitada'],['Apática','indiferente, sem interesse/entusiasmo'],['Pesada','triste, abatida, emocionalmente carregada']];function show(id){views.forEach(v=>v.classList.toggle('active',v.id===id));nav.forEach(b=>b.classList.toggle('active',b.dataset.view===id));if(id==='history')renderList()}function fmtDate(s){return new Intl.DateTimeFormat('pt-BR',{day:'numeric',month:'long',year:'numeric',timeZone:'UTC'}).format(new Date(s+'T12:00:00Z'))}function esc(s){return String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}async function load(){const{data,error}=await sb.from('daily_checkins').select('*').order('checkin_date',{ascending:false});if(error){console.error(error);return}items=data||[];renderCalendar();if(document.getElementById('history')?.classList.contains('active'))renderList()}function renderCalendar(){const y=month.getFullYear(),m=month.getMonth(),label=new Intl.DateTimeFormat('pt-BR',{month:'long',year:'numeric'}).format(new Date(y,m,1));$('monthTitle').textContent=label[0].toUpperCase()+label.slice(1);const first=new Date(y,m,1).getDay(),last=new Date(y,m+1,0).getDate(),map=new Map(items.map(x=>[x.checkin_date,x]));let html='';for(let i=0;i<first;i++)html+='<span class="day empty"></span>';for(let d=1;d<=last;d++){const k=`${y}-${pad(m+1)}-${pad(d)}`,done=map.has(k);html+=done?`<button class="day done ${k===today()?'today':''}" data-date="${k}"><img src="${window.GI_AVATAR||''}" alt=""><span class="day-number">${d}</span></button>`:`<button class="day ${k===today()?'today':''}" data-date="${k}">${d}</button>`}$('grid').innerHTML=html;$('grid').querySelectorAll('[data-date]').forEach(b=>b.onclick=()=>openModal(b.dataset.date));const current=items.filter(x=>x.checkin_date.startsWith(`${y}-${pad(m+1)}`));$('total').textContent=items.length;$('days').textContent=new Set(items.map(x=>x.checkin_date)).size;$('monthCount').textContent=current.length}function makeMenu(id,options,set){$(id).innerHTML=options.map(([name,desc])=>`<button class="multi-option" type="button" data-value="${name}"><span class="check">✓</span><span><strong>${name}</strong><small>${desc}</small></span></button>`).join('');$(id).onclick=e=>{const b=e.target.closest('.multi-option');if(!b)return;const v=b.dataset.value;set.has(v)?set.delete(v):set.add(v);paintMenus()}}function paintMenus(){[['mindMenu',mindStates,'mindTrigger'],['soulMenu',soulStates,'soulTrigger']].forEach(([menu,set,trigger])=>{[...$(menu).children].forEach(b=>b.classList.toggle('selected',set.has(b.dataset.value)));const arr=[...set];$(trigger).querySelector('span').textContent=arr.length?arr.join(', '):'Selecionar'})}makeMenu('mindMenu',MIND,mindStates);makeMenu('soulMenu',SOUL,soulStates);function openModal(date){selectedDate=date;const old=items.find(x=>x.checkin_date===date);mindStates.clear();soulStates.clear();(old?.mind_states||[]).forEach(x=>mindStates.add(x));(old?.soul_states||[]).forEach(x=>soulStates.add(x));$('formDate').textContent=fmtDate(date);$('joy').value=old?.joy_reason||old?.day_saver||'';$('note').value=old?.note||'';paintMenus();$('modal').hidden=false;document.body.style.overflow='hidden'}function closeModal(){$('modal').hidden=true;document.body.style.overflow='';document.querySelectorAll('.multi').forEach(x=>x.classList.remove('open'))}async function save(){const payload={user_id:user.id,checkin_date:selectedDate,mind_states:[...mindStates],soul_states:[...soulStates],joy_reason:$('joy').value.trim()||null,note:$('note').value.trim()||null,mind_score:null,soul_score:null,updated_at:new Date().toISOString()};const{error}=await sb.from('daily_checkins').upsert(payload,{onConflict:'user_id,checkin_date'});if(error){console.error(error);alert('Não consegui salvar.');return}await load();closeModal()}function renderList(){$('list').innerHTML=items.length?items.map(x=>{const tags=[...(x.mind_states||[]),...(x.soul_states||[])];return`<article class="entry" data-date="${x.checkin_date}"><div class="date">${fmtDate(x.checkin_date)}</div>${tags.length?`<div class="tags">${tags.map(t=>`<span class="tag">${esc(t)}</span>`).join('')}</div>`:''}${(x.joy_reason||x.day_saver)?`<div class="joy">${esc(x.joy_reason||x.day_saver)}</div>`:''}${x.note?`<div class="note">${esc(x.note.slice(0,180))}${x.note.length>180?'…':''}</div>`:''}</article>`}).join(''):'<p style="color:var(--muted)">Seus check-ins aparecerão aqui.</p>';$('list').querySelectorAll('[data-date]').forEach(x=>x.onclick=()=>openModal(x.dataset.date))}nav.forEach(b=>b.onclick=()=>show(b.dataset.view));$('prev').onclick=()=>{month=new Date(month.getFullYear(),month.getMonth()-1,1);renderCalendar()};$('next').onclick=()=>{month=new Date(month.getFullYear(),month.getMonth()+1,1);renderCalendar()};$('closeModal').onclick=closeModal;$('modalBackdrop').onclick=closeModal;$('save').onclick=save;['mindTrigger','soulTrigger'].forEach(id=>$(id).onclick=e=>{e.stopPropagation();const multi=$(id).closest('.multi'),was=multi.classList.contains('open');document.querySelectorAll('.multi').forEach(x=>x.classList.remove('open'));if(!was)multi.classList.add('open')});document.addEventListener('click',e=>{if(!e.target.closest('.multi'))document.querySelectorAll('.multi').forEach(x=>x.classList.remove('open'))});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()});function theme(t){document.documentElement.dataset.theme=t;localStorage.setItem('gi-theme',t);localStorage.setItem('escrita-theme',t)}theme(localStorage.getItem('gi-theme')||(matchMedia('(prefers-color-scheme:dark)').matches?'dark':'light'));$('theme').onclick=()=>theme(document.documentElement.dataset.theme==='dark'?'light':'dark');async function init(){const{data:{session}}=await sb.auth.getSession();if(!session){location.replace('/?next='+encodeURIComponent(location.pathname));return}user=session.user;$('calendarAvatar').src=window.GI_AVATAR||'';await load();show('calendar')}init();