fetch('./app.js').then(r=>r.text()).then(code=>{code=code.replace("if(!name)return, split=id=>$(id).value.split(',').map(x=>x.trim()).filter(Boolean);","if(!name)return; const split=id=>$(id).value.split(',').map(x=>x.trim()).filter(Boolean);");(0,eval)(code)}).catch(err=>{console.error(err);document.body.insertAdjacentHTML('beforeend','<div style="padding:20px">Não consegui carregar a Academia.</div>')});